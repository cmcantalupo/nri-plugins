# Rendered by cluster/10-baremetal-provision.sh (`seed`). Minimal NoCloud
# meta-data; the real configuration lives in user-data.
instance-id: aet-baremetal/@REIMAGE_HOSTNAME@
local-hostname: @REIMAGE_HOSTNAME@
